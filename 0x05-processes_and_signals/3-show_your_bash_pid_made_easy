#!/usr/bin/env bash
# Get the PID of the current script

# Get the PID of processes containing the word "bash" in their name
bash_pids=$(pgrep -f bash)

# Loop through each PID and get the process name
for pid in $bash_pids; do
    # Get the process name
    process_name=$(cat /proc/"$pid"/cmdline | tr '\0' ' ' | awk '{print $1}')

    # Display PID and process name
    echo "$pid $process_name"
done
